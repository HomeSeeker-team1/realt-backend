const regx = {
  name: () => /^((?![\\s.]+$)([a-zA-Z' -]{1,80})|(?![\\s.]+$)([а-яА-ЯЁёІіЇїҐґЄє' -]{1,80}))$/,
  patronymic: () => /^((?![\\s.]+$)([a-zA-Z' -]{0,80})|(?![\\s.]+$)([а-яА-ЯЁёІіЇїҐґЄє' -]{0,80}))$/,
};

export default regx;
